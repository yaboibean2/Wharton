[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not live_api"
markers =
    integration: integration tests spanning multiple project components
    live_api: tests that call external APIs and consume network/API quota
